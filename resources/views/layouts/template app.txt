<!DOCTYPE html>
<html lang="id">
<head>
    <link href="https://fonts.googleapis.com/css2?family=Syne:wght@400;500;600;700;800&family=DM+Sans:ital,wght@0,300;0,400;0,500;0,600;1,300&family=JetBrains+Mono:wght@400;500;600&display=swap" rel="stylesheet">
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="csrf-token" content="{{ csrf_token() }}">
    <title>{{ $title ?? 'Dashboard' }} — MasterIP</title>
    @vite(['resources/css/app.css', 'resources/js/app.js'])

    <style>
        :root {
            --navy:        #060d1f;
            --navy-2:      #0d1a35;
            --blue:        #3b82f6;
            --blue-glow:   rgba(59,130,246,0.35);
            --cyan:        #06b6d4;
            --cyan-glow:   rgba(6,182,212,0.30);
            --violet:      #8b5cf6;
            --glass:       rgba(255,255,255,0.52);
            --glass-dark:  rgba(10,18,40,0.72);
            --glass-border: rgba(255,255,255,0.32);
            --text-primary: #0f172a;
            --text-muted:   #64748b;
            --radius-nav:  20px;
            --nav-h:       72px;
        }

        *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
        html { scroll-behavior: smooth; }

        body {
            font-family: 'DM Sans', sans-serif;
            min-height: 100vh;
            background: url('{{ asset('aset/bgwhite.jpg') }}') center/cover no-repeat fixed;
            overflow-x: hidden;
        }

        /* ═══════════════════════════════════════════
           AMBIENT BG ORBS
        ═══════════════════════════════════════════ */
        .bg-orb {
            position: fixed; border-radius: 50%;
            pointer-events: none; z-index: 0;
            filter: blur(80px); opacity: 0;
            animation: orbFloat 18s ease-in-out infinite;
        }
        .bg-orb-1 { width:500px;height:500px;background:radial-gradient(circle,rgba(59,130,246,0.12) 0%,transparent 70%);top:-150px;right:-100px;animation-delay:0s;opacity:1; }
        .bg-orb-2 { width:400px;height:400px;background:radial-gradient(circle,rgba(6,182,212,0.10) 0%,transparent 70%);bottom:20%;left:-100px;animation-delay:-6s;opacity:1; }
        .bg-orb-3 { width:300px;height:300px;background:radial-gradient(circle,rgba(139,92,246,0.08) 0%,transparent 70%);top:50%;left:50%;animation-delay:-12s;opacity:1; }
        @keyframes orbFloat {
            0%,100% { transform:translate(0,0) scale(1); }
            33%     { transform:translate(30px,-40px) scale(1.05); }
            66%     { transform:translate(-20px,20px) scale(0.95); }
        }

        /* ═══════════════════════════════════════════
           PAGE TRANSITION — ultra-fast liquid wipe
        ═══════════════════════════════════════════ */
        #page-transition {
            position: fixed; inset: 0; z-index: 9999;
            pointer-events: none;
        }

        /* Single full-screen curtain using clip-path */
        .pt-curtain {
            position: absolute; inset: 0;
            background: linear-gradient(135deg,
                #060d1f 0%, #0d2157 30%, #06b6d4 60%, #3b82f6 80%, #060d1f 100%);
            background-size: 300% 300%;
            animation: curtainGradShift 2s ease infinite;
            clip-path: polygon(0 100%, 100% 100%, 100% 100%, 0 100%);
            transition: clip-path 0.32s cubic-bezier(0.76, 0, 0.24, 1);
            will-change: clip-path;
        }
        .pt-curtain-2 {
            position: absolute; inset: 0;
            background: linear-gradient(135deg,
                #06b6d4 0%, #3b82f6 40%, #8b5cf6 70%, #060d1f 100%);
            clip-path: polygon(0 100%, 100% 100%, 100% 100%, 0 100%);
            transition: clip-path 0.28s cubic-bezier(0.76, 0, 0.24, 1) 0.04s;
            will-change: clip-path;
            opacity: 0.7;
        }

        @keyframes curtainGradShift {
            0%,100% { background-position: 0% 50%; }
            50%     { background-position: 100% 50%; }
        }

        /* Active = slide UP from bottom */
        #page-transition.active .pt-curtain {
            clip-path: polygon(0 0, 100% 0, 100% 100%, 0 100%);
        }
        #page-transition.active .pt-curtain-2 {
            clip-path: polygon(0 0, 100% 0, 100% 100%, 0 100%);
        }
        /* Fade-out = slide UP, disappear off top */
        #page-transition.fade-out .pt-curtain {
            clip-path: polygon(0 0, 100% 0, 100% 0, 0 0);
            transition: clip-path 0.28s cubic-bezier(0.76, 0, 0.24, 1) 0s;
        }
        #page-transition.fade-out .pt-curtain-2 {
            clip-path: polygon(0 0, 100% 0, 100% 0, 0 0);
            transition: clip-path 0.24s cubic-bezier(0.76, 0, 0.24, 1) 0.02s;
        }

        /* Particle burst on transition */
        .pt-particles {
            position: absolute; inset: 0; pointer-events: none;
            overflow: hidden; opacity: 0;
            transition: opacity 0.1s;
        }
        #page-transition.active .pt-particles { opacity: 1; }
        .pt-particle {
            position: absolute;
            width: 3px; height: 3px; border-radius: 50%;
            background: #06b6d4;
            animation: ptParticle 0.5s ease-out forwards;
        }
        @keyframes ptParticle {
            0%   { transform: scale(0) translate(0,0); opacity: 1; }
            100% { transform: scale(1) translate(var(--tx), var(--ty)); opacity: 0; }
        }

        /* Logo in center during transition */
        #pt-logo {
            position: fixed; inset: 0; z-index: 10000;
            display: flex; align-items: center; justify-content: center;
            pointer-events: none;
            opacity: 0;
            transition: opacity 0.15s ease;
        }
        #pt-logo.visible { opacity: 1; }
        #pt-logo .pt-logo-ring {
            position: relative;
            width: 64px; height: 64px;
            display: flex; align-items: center; justify-content: center;
        }
        #pt-logo .pt-logo-ring::before,
        #pt-logo .pt-logo-ring::after {
            content: '';
            position: absolute; inset: 0;
            border-radius: 50%;
            border: 2px solid transparent;
        }
        #pt-logo .pt-logo-ring::before {
            border-top-color: #06b6d4;
            border-right-color: rgba(6,182,212,0.3);
            animation: ptSpin 0.6s linear infinite;
        }
        #pt-logo .pt-logo-ring::after {
            inset: 6px;
            border-bottom-color: #3b82f6;
            border-left-color: rgba(59,130,246,0.3);
            animation: ptSpin 0.4s linear infinite reverse;
        }
        @keyframes ptSpin { to { transform: rotate(360deg); } }
        #pt-logo svg {
            width: 26px; height: 26px; color: #fff; position: relative; z-index: 1;
            filter: drop-shadow(0 0 12px rgba(6,182,212,1));
        }

        /* ═══════════════════════════════════════════
           SCROLL PROGRESS BAR
        ═══════════════════════════════════════════ */
        #scroll-progress {
            position: fixed; top: 0; left: 0; height: 3px; z-index: 9998;
            background: linear-gradient(90deg, #3b82f6, #06b6d4, #8b5cf6, #06b6d4, #3b82f6);
            background-size: 300% 100%;
            width: 0%;
            transition: width 0.08s linear;
            box-shadow: 0 0 12px rgba(6,182,212,0.9), 0 0 24px rgba(59,130,246,0.5);
            animation: progressGrad 3s linear infinite;
        }
        @keyframes progressGrad {
            0%   { background-position: 0% 50%; }
            100% { background-position: 300% 50%; }
        }
        /* Glow head */
        #scroll-progress::after {
            content: '';
            position: absolute; right: 0; top: 50%;
            transform: translateY(-50%);
            width: 12px; height: 12px; border-radius: 50%;
            background: #06b6d4;
            box-shadow: 0 0 10px 4px rgba(6,182,212,0.8);
        }

        /* ═══════════════════════════════════════════
           NAVBAR
        ═══════════════════════════════════════════ */
        #navbar {
            position: fixed; top: 0; left: 0; width: 100%;
            z-index: 50;
            transition: transform 0.4s cubic-bezier(0.4,0,0.2,1);
        }
        #navbar.hidden-bar { transform: translateY(-110%); }

        .nav-inner {
            margin: 10px 20px;
            background: var(--glass);
            backdrop-filter: blur(32px) saturate(220%);
            -webkit-backdrop-filter: blur(32px) saturate(220%);
            border: 1px solid var(--glass-border);
            border-radius: var(--radius-nav);
            padding: 8px 16px;
            display: flex; align-items: center; justify-content: space-between;
            box-shadow:
                0 8px 40px rgba(15,23,42,0.10),
                0 2px 8px rgba(59,130,246,0.06),
                inset 0 1px 0 rgba(255,255,255,0.65),
                inset 0 -1px 0 rgba(255,255,255,0.20);
            position: relative; overflow: hidden;
            transition: box-shadow 0.35s ease, border-color 0.35s ease;
        }

        /* Animated gradient border */
        .nav-inner::before {
            content: '';
            position: absolute; top: 0; left: 0; right: 0; height: 1.5px;
            background: linear-gradient(90deg,
                transparent 0%,
                rgba(59,130,246,0.8) 20%,
                rgba(6,182,212,0.9) 40%,
                rgba(139,92,246,0.8) 60%,
                rgba(6,182,212,0.9) 80%,
                transparent 100%);
            background-size: 200% 100%;
            animation: navBorderFlow 3s linear infinite;
        }
        @keyframes navBorderFlow {
            0%   { background-position: 100% 0; }
            100% { background-position: -100% 0; }
        }

        /* Aurora shimmer behind nav */
        .nav-inner::after {
            content: '';
            position: absolute; inset: 0;
            background: linear-gradient(105deg,
                rgba(59,130,246,0.04) 0%,
                rgba(6,182,212,0.06) 40%,
                rgba(139,92,246,0.04) 80%,
                transparent 100%);
            background-size: 300% 100%;
            animation: navAurora 6s ease-in-out infinite;
            pointer-events: none;
        }
        @keyframes navAurora {
            0%,100% { background-position: 0% 50%; }
            50%     { background-position: 100% 50%; }
        }

        .nav-inner:hover {
            box-shadow:
                0 12px 48px rgba(15,23,42,0.14),
                0 4px 20px rgba(59,130,246,0.14),
                inset 0 1px 0 rgba(255,255,255,0.75),
                inset 0 -1px 0 rgba(255,255,255,0.25);
            border-color: rgba(255,255,255,0.50);
        }

        /* Brand */
        .nav-brand {
            display: flex; align-items: center; gap: 10px;
            text-decoration: none; flex-shrink: 0;
        }
        .brand-icon {
            width: 40px; height: 40px;
            background: linear-gradient(135deg, #2563eb, #06b6d4);
            border-radius: 12px;
            display: flex; align-items: center; justify-content: center;
            box-shadow: 0 4px 14px rgba(59,130,246,0.45), 0 1px 3px rgba(6,182,212,0.25);
            position: relative; overflow: hidden;
            transition: transform 0.3s cubic-bezier(0.34,1.56,0.64,1), box-shadow 0.3s ease;
        }
        /* Holographic sheen */
        .brand-icon::before {
            content: '';
            position: absolute; inset: 0;
            background: conic-gradient(from 0deg,
                rgba(255,255,255,0) 0deg,
                rgba(255,255,255,0.25) 60deg,
                rgba(255,255,255,0) 120deg,
                rgba(255,255,255,0) 360deg);
            animation: brandSpin 2s linear infinite;
        }
        /* Inner glow */
        .brand-icon::after {
            content: '';
            position: absolute; inset: 2px;
            border-radius: 10px;
            background: linear-gradient(135deg, rgba(255,255,255,0.15), transparent);
            pointer-events: none;
        }
        @keyframes brandSpin {
            from { transform: rotate(0deg); }
            to   { transform: rotate(360deg); }
        }
        .nav-brand:hover .brand-icon {
            transform: scale(1.1) rotate(-6deg);
            box-shadow: 0 8px 28px rgba(59,130,246,0.6), 0 2px 8px rgba(6,182,212,0.4);
        }
        .brand-icon svg { width: 18px; height: 18px; color: #fff; position: relative; z-index: 1; }
        .brand-text {
            font-family: 'Syne', sans-serif;
            font-size: 1.18rem; font-weight: 800;
            background: linear-gradient(135deg, var(--navy), #1d4ed8, #0891b2);
            -webkit-background-clip: text; -webkit-text-fill-color: transparent;
            letter-spacing: -0.03em;
        }
        .brand-badge {
            font-family: 'JetBrains Mono', monospace;
            font-size: 0.52rem; font-weight: 600;
            background: linear-gradient(135deg, #3b82f6, #06b6d4);
            color: #fff; border-radius: 5px;
            padding: 2px 6px; letter-spacing: 0.08em;
            transform: translateY(-5px);
            box-shadow: 0 2px 8px rgba(59,130,246,0.3);
            animation: badgePop 3s ease-in-out infinite;
        }
        @keyframes badgePop {
            0%,100% { transform: translateY(-5px) scale(1); }
            50%     { transform: translateY(-7px) scale(1.06); box-shadow: 0 4px 14px rgba(59,130,246,0.5); }
        }

        /* Nav links */
        .nav-links {
            display: flex; align-items: center; gap: 2px; list-style: none;
        }
        .nav-links a {
            position: relative;
            display: flex; align-items: center; gap: 6px;
            padding: 7px 12px; border-radius: 11px;
            font-size: 0.855rem; font-weight: 500;
            color: #334155; text-decoration: none;
            transition: color 0.2s ease, transform 0.25s cubic-bezier(0.34,1.56,0.64,1);
            white-space: nowrap; overflow: hidden;
            isolation: isolate;
        }
        .nav-links a svg {
            width: 15px; height: 15px; flex-shrink: 0;
            transition: transform 0.3s cubic-bezier(0.34,1.56,0.64,1), color 0.2s;
        }

        /* Liquid fill hover */
        .nav-links a .nav-fill {
            position: absolute; inset: 0; border-radius: 11px;
            background: linear-gradient(135deg, rgba(59,130,246,0.12), rgba(6,182,212,0.10));
            transform: scaleX(0) scaleY(0.5);
            transform-origin: center;
            transition: transform 0.3s cubic-bezier(0.34,1.56,0.64,1), opacity 0.2s;
            opacity: 0; z-index: -1; border-radius: 11px;
        }
        .nav-links a:hover .nav-fill,
        .nav-links a.active .nav-fill {
            transform: scaleX(1) scaleY(1);
            opacity: 1;
        }

        .nav-links a:hover { color: #1d4ed8; transform: translateY(-2px); }
        .nav-links a:hover svg { transform: scale(1.18) rotate(-6deg); color: #1d4ed8; }
        .nav-links a.active { color: #1d4ed8; font-weight: 600; }
        .nav-links a.active svg { color: #1d4ed8; }

        /* Active underline glow */
        .nav-links a::after {
            content: '';
            position: absolute; bottom: 3px; left: 50%;
            width: 0; height: 2px;
            background: linear-gradient(90deg, #3b82f6, #06b6d4);
            border-radius: 2px;
            transform: translateX(-50%);
            transition: width 0.3s cubic-bezier(0.4,0,0.2,1);
            box-shadow: 0 0 8px rgba(6,182,212,0.7);
        }
        .nav-links a:hover::after,
        .nav-links a.active::after { width: 50%; }

        /* Live dot */
        .nav-net-dot {
            width: 6px; height: 6px; border-radius: 50%;
            background: #22c55e;
            box-shadow: 0 0 0 0 rgba(34,197,94,0.7);
            animation: sonarPing 2s cubic-bezier(0,0,0.2,1) infinite;
            flex-shrink: 0;
        }
        @keyframes sonarPing {
            0%   { box-shadow: 0 0 0 0 rgba(34,197,94,0.7); }
            70%  { box-shadow: 0 0 0 8px rgba(34,197,94,0); }
            100% { box-shadow: 0 0 0 0 rgba(34,197,94,0); }
        }

        .nav-divider {
            width: 1px; height: 22px;
            background: linear-gradient(to bottom, transparent, rgba(148,163,184,0.4), transparent);
            margin: 0 5px;
        }

        .nav-logout {
            display: flex; align-items: center; gap: 6px;
            padding: 7px 13px; border-radius: 11px;
            font-size: 0.855rem; font-weight: 500;
            color: #ef4444; text-decoration: none;
            transition: color 0.2s ease, transform 0.25s cubic-bezier(0.34,1.56,0.64,1);
            position: relative; overflow: hidden; isolation: isolate;
        }
        .nav-logout .nav-fill {
            position: absolute; inset: 0; border-radius: 11px;
            background: rgba(239,68,68,0.09);
            transform: scaleX(0) scaleY(0.5); transform-origin: center;
            transition: transform 0.3s cubic-bezier(0.34,1.56,0.64,1);
            opacity: 0; z-index: -1;
        }
        .nav-logout:hover .nav-fill { transform: scaleX(1) scaleY(1); opacity: 1; }
        .nav-logout svg { width: 15px; height: 15px; transition: transform 0.25s cubic-bezier(0.34,1.56,0.64,1); }
        .nav-logout:hover { color: #dc2626; transform: translateY(-2px); }
        .nav-logout:hover svg { transform: translateX(4px); }

        /* User chip */
        .user-chip {
            display: flex; align-items: center; gap: 9px;
            background: rgba(15,23,42,0.05);
            border: 1px solid rgba(15,23,42,0.09);
            border-radius: 40px; padding: 4px 13px 4px 4px;
            cursor: default;
            transition: background 0.25s ease, border-color 0.25s ease, box-shadow 0.25s ease, transform 0.25s cubic-bezier(0.34,1.56,0.64,1);
        }
        .user-chip:hover {
            background: rgba(59,130,246,0.07);
            border-color: rgba(59,130,246,0.25);
            box-shadow: 0 2px 16px rgba(59,130,246,0.12);
            transform: translateY(-1px);
        }
        .user-avatar {
            width: 30px; height: 30px; border-radius: 50%;
            background: linear-gradient(135deg, #3b82f6, #06b6d4);
            display: flex; align-items: center; justify-content: center;
            font-family: 'Syne', sans-serif;
            font-size: 0.7rem; font-weight: 800; color: #fff; flex-shrink: 0;
            animation: avatarPulse 4s ease infinite;
            position: relative;
        }
        .user-avatar::after {
            content: '';
            position: absolute; inset: -3px; border-radius: 50%;
            border: 1.5px solid transparent;
            background: linear-gradient(135deg, #3b82f6, #06b6d4) border-box;
            -webkit-mask: linear-gradient(#fff 0 0) padding-box, linear-gradient(#fff 0 0);
            -webkit-mask-composite: destination-out;
            mask-composite: exclude;
            opacity: 0;
            transition: opacity 0.3s;
        }
        .user-chip:hover .user-avatar::after { opacity: 1; }
        @keyframes avatarPulse {
            0%,100% { box-shadow: 0 0 0 0 rgba(59,130,246,0.4); }
            50%     { box-shadow: 0 0 0 5px rgba(59,130,246,0.0); }
        }
        .user-name { font-size: 0.8rem; font-weight: 600; color: #1e293b; line-height: 1.2; }
        .user-role { font-size: 0.67rem; font-weight: 400; color: #64748b; font-family: 'JetBrains Mono', monospace; }

        /* ═══════════════════════════════════════════
           CONTENT
        ═══════════════════════════════════════════ */
        main {
            max-width: 1300px; margin: 0 auto;
            padding: 104px 24px 48px;
            position: relative; z-index: 1;
        }

        /* ═══════════════════════════════════════════
           FOOTER — completely redesigned
        ═══════════════════════════════════════════ */
        footer {
            margin-top: 80px; position: relative; z-index: 1; overflow: hidden;
        }

        /* Dramatic separator above footer */
        .footer-separator {
            height: 120px; position: relative; overflow: hidden;
            background: transparent;
        }
        .footer-separator svg {
            position: absolute; bottom: 0; left: 0; width: 100%; height: 100%;
        }

        .footer-inner {
            background: linear-gradient(180deg,
                rgba(4,10,25,0.97) 0%,
                rgba(6,13,31,0.99) 100%);
            backdrop-filter: blur(24px);
            border-top: none;
            padding: 0 24px 0;
            position: relative;
        }

        /* Animated top glow border */
        .footer-inner::before {
            content: '';
            position: absolute; top: 0; left: 0; right: 0; height: 2px;
            background: linear-gradient(90deg,
                transparent 0%,
                #3b82f6 15%,
                #06b6d4 35%,
                #8b5cf6 55%,
                #06b6d4 75%,
                #3b82f6 85%,
                transparent 100%);
            background-size: 200% 100%;
            animation: footerBorderFlow 4s linear infinite;
            box-shadow: 0 0 20px rgba(6,182,212,0.5), 0 0 40px rgba(59,130,246,0.3);
        }
        @keyframes footerBorderFlow {
            0%   { background-position: 100% 0; }
            100% { background-position: -100% 0; }
        }

        /* Huge ambient glow inside footer */
        .footer-glow {
            position: absolute; top: -60px; left: 50%; transform: translateX(-50%);
            width: 800px; height: 200px;
            background: radial-gradient(ellipse, rgba(59,130,246,0.12) 0%, rgba(6,182,212,0.08) 40%, transparent 70%);
            pointer-events: none;
            animation: footerGlowPulse 5s ease-in-out infinite;
        }
        @keyframes footerGlowPulse {
            0%,100% { opacity: 0.7; transform: translateX(-50%) scaleX(1); }
            50%     { opacity: 1;   transform: translateX(-50%) scaleX(1.1); }
        }

        /* Matrix rain canvas */
        #footer-matrix {
            position: absolute; top: 0; left: 0; width: 100%; height: 100%;
            opacity: 0.03; pointer-events: none;
        }

        /* Grid lines */
        .footer-grid-bg {
            position: absolute; inset: 0; pointer-events: none;
            background-image:
                linear-gradient(rgba(59,130,246,0.06) 1px, transparent 1px),
                linear-gradient(90deg, rgba(59,130,246,0.06) 1px, transparent 1px);
            background-size: 60px 60px;
            mask-image: linear-gradient(to bottom, transparent, rgba(0,0,0,0.5) 30%, rgba(0,0,0,0.5) 70%, transparent);
        }

        .footer-content-wrap {
            position: relative; z-index: 2;
            max-width: 1300px; margin: 0 auto;
            padding: 56px 0 0;
        }

        /* Ticker */
        .footer-ticker {
            overflow: hidden; width: 100%;
            border-top: 1px solid rgba(255,255,255,0.04);
            border-bottom: 1px solid rgba(255,255,255,0.04);
            padding: 10px 0; margin-bottom: 48px;
            position: relative;
        }
        .footer-ticker::before,
        .footer-ticker::after {
            content: '';
            position: absolute; top: 0; height: 100%; width: 100px; z-index: 1;
            pointer-events: none;
        }
        .footer-ticker::before {
            left: 0; background: linear-gradient(to right, rgba(4,10,25,0.98), transparent);
        }
        .footer-ticker::after {
            right: 0; background: linear-gradient(to left, rgba(4,10,25,0.98), transparent);
        }
        .ticker-track {
            display: flex; gap: 32px;
            animation: ticker 20s linear infinite;
            white-space: nowrap;
        }
        .ticker-track:hover { animation-play-state: paused; }
        .ticker-track span {
            font-family: 'Syne', sans-serif;
            font-size: 0.68rem; font-weight: 700;
            color: rgba(96,165,250,0.4);
            letter-spacing: 0.18em; text-transform: uppercase;
            transition: color 0.2s;
        }
        .ticker-track:hover span { color: rgba(96,165,250,0.7); }
        .ticker-track span.ticker-sep { color: rgba(6,182,212,0.3); }
        @keyframes ticker { 0% { transform: translateX(0); } 100% { transform: translateX(-50%); } }

        /* Main footer grid */
        .footer-grid {
            display: grid;
            grid-template-columns: 2fr 1fr 1fr;
            gap: 48px; margin-bottom: 48px;
        }

        .footer-brand .brand-text-footer {
            font-family: 'Syne', sans-serif;
            font-size: 1.6rem; font-weight: 800;
            background: linear-gradient(90deg, #60a5fa, #06b6d4, #818cf8, #60a5fa);
            background-size: 300% auto;
            -webkit-background-clip: text; -webkit-text-fill-color: transparent;
            margin-bottom: 12px;
            animation: gradientShift 5s linear infinite;
            letter-spacing: -0.03em;
        }
        @keyframes gradientShift {
            0%   { background-position: 0% center; }
            100% { background-position: 300% center; }
        }
        .footer-brand p {
            font-size: 0.83rem; color: #64748b; line-height: 1.8; max-width: 260px;
        }
        .footer-status {
            display: flex; align-items: center; gap: 16px; margin-top: 24px;
        }
        .status-item {
            display: flex; align-items: center; gap: 7px;
            font-family: 'JetBrains Mono', monospace;
            font-size: 0.65rem; color: #475569;
            background: rgba(255,255,255,0.03);
            border: 1px solid rgba(255,255,255,0.06);
            border-radius: 20px; padding: 4px 10px;
            transition: border-color 0.2s, color 0.2s;
        }
        .status-item:hover { border-color: rgba(34,197,94,0.3); color: #94a3b8; }
        .status-dot {
            width: 6px; height: 6px; border-radius: 50%;
        }
        .status-dot.online {
            background: #22c55e;
            box-shadow: 0 0 8px rgba(34,197,94,0.6);
            animation: sonarPing 2.5s ease-in-out infinite;
        }
        .status-dot.active-blue {
            background: #3b82f6;
            box-shadow: 0 0 8px rgba(59,130,246,0.6);
            animation: sonarPing 2.5s ease-in-out infinite 0.8s;
        }

        .footer-col h4 {
            font-family: 'Syne', sans-serif;
            font-size: 0.72rem; font-weight: 700;
            color: #60a5fa; letter-spacing: 0.14em;
            text-transform: uppercase; margin-bottom: 20px;
            display: flex; align-items: center; gap: 10px;
        }
        .footer-col h4::after {
            content: '';
            flex: 1; height: 1px;
            background: linear-gradient(90deg, rgba(96,165,250,0.4), transparent);
        }
        .footer-col ul { list-style: none; display: flex; flex-direction: column; gap: 4px; }
        .footer-col ul a {
            font-size: 0.83rem; color: #64748b; text-decoration: none;
            transition: color 0.2s ease, padding-left 0.2s ease;
            display: flex; align-items: center; gap: 8px;
            padding: 5px 8px; border-radius: 7px;
            position: relative; overflow: hidden;
            transition: color 0.2s, background 0.2s, padding-left 0.2s;
        }
        .footer-col ul a::before {
            content: '›'; font-size: 1rem; color: #3b82f6;
            opacity: 0; transform: translateX(-6px);
            transition: opacity 0.2s ease, transform 0.2s ease;
        }
        .footer-col ul a:hover {
            color: #cbd5e1; padding-left: 14px;
            background: rgba(59,130,246,0.05);
        }
        .footer-col ul a:hover::before { opacity: 1; transform: translateX(0); }

        /* Futuristic footer bottom bar */
        .footer-bottom-wrap {
            position: relative; overflow: hidden;
        }
        .footer-bottom-wrap::before {
            content: '';
            position: absolute; top: 0; left: 0; right: 0; height: 1px;
            background: linear-gradient(90deg, transparent, rgba(59,130,246,0.3), rgba(6,182,212,0.3), transparent);
        }
        .footer-bottom {
            padding: 24px 0 28px;
            display: flex; align-items: center; justify-content: space-between;
            flex-wrap: wrap; gap: 12px;
        }
        .footer-bottom span { font-size: 0.74rem; color: #334155; }
        .footer-by {
            font-size: 0.74rem; color: #334155;
            display: flex; align-items: center; gap: 8px;
        }
        .footer-by strong {
            font-family: 'Syne', sans-serif; color: #60a5fa; font-weight: 800;
            letter-spacing: -0.02em;
            background: linear-gradient(90deg, #60a5fa, #06b6d4);
            -webkit-background-clip: text; -webkit-text-fill-color: transparent;
        }
        .footer-version {
            background: rgba(59,130,246,0.10);
            color: #60a5fa; font-size: 0.63rem; font-weight: 600;
            padding: 2px 8px; border-radius: 6px;
            border: 1px solid rgba(59,130,246,0.20);
            font-family: 'JetBrains Mono', monospace;
            transition: background 0.2s, box-shadow 0.2s;
        }
        .footer-version:hover {
            background: rgba(59,130,246,0.18);
            box-shadow: 0 0 12px rgba(59,130,246,0.3);
        }

        /* Heartbeat in footer */
        .footer-heartbeat {
            display: flex; align-items: center; gap: 10px;
            font-family: 'JetBrains Mono', monospace;
            font-size: 0.65rem; color: #1e3a5f;
        }
        .hb-line {
            display: flex; align-items: center; gap: 0;
            height: 20px; overflow: hidden;
        }
        .hb-line svg {
            width: 80px; height: 20px;
            stroke: #22c55e; fill: none; stroke-width: 1.5;
            stroke-dasharray: 200;
            stroke-dashoffset: 200;
            animation: drawHB 2s linear infinite;
        }
        @keyframes drawHB {
            0%   { stroke-dashoffset: 200; opacity: 0.3; }
            30%  { opacity: 1; }
            100% { stroke-dashoffset: -200; opacity: 0.3; }
        }

        /* ═══════════════════════════════════════════
           TOAST NOTIFICATIONS
        ═══════════════════════════════════════════ */
        #toast-container {
            position: fixed; bottom: 28px; right: 28px;
            z-index: 9900;
            display: flex; flex-direction: column; gap: 10px;
            pointer-events: none;
        }
        .toast {
            background: var(--glass-dark);
            backdrop-filter: blur(20px);
            border: 1px solid rgba(255,255,255,0.12);
            border-radius: 14px; padding: 12px 18px;
            display: flex; align-items: center; gap: 10px;
            color: #e2e8f0; font-size: 0.84rem;
            box-shadow: 0 8px 32px rgba(0,0,0,0.3), 0 2px 8px rgba(0,0,0,0.2);
            pointer-events: auto;
            transform: translateX(110%); opacity: 0;
            transition: transform 0.35s cubic-bezier(0.34,1.56,0.64,1), opacity 0.25s ease;
            max-width: 320px;
        }
        .toast.show { transform: translateX(0); opacity: 1; }
        .toast-icon {
            width: 28px; height: 28px; border-radius: 8px; flex-shrink: 0;
            display: flex; align-items: center; justify-content: center;
        }
        .toast-icon.info { background: rgba(59,130,246,0.2); color: #60a5fa; }
        .toast-icon.ok   { background: rgba(34,197,94,0.2); color: #4ade80; }
        .toast-icon svg  { width: 14px; height: 14px; }

        /* ═══════════════════════════════════════════
           RESPONSIVE
        ═══════════════════════════════════════════ */
        @media (max-width: 768px) {
            .nav-inner  { margin: 8px 10px; padding: 7px 12px; }
            .nav-links  { gap: 0; }
            .nav-links a span { display: none; }
            .footer-grid { grid-template-columns: 1fr 1fr; }
            .footer-brand { grid-column: 1 / -1; }
        }
        @media (max-width: 480px) {
            .user-name, .user-role { display: none; }
            .nav-divider { display: none; }
            .footer-grid { grid-template-columns: 1fr; }
        }
    </style>
</head>
<body>

    {{-- Ambient Orbs --}}
    <div class="bg-orb bg-orb-1"></div>
    <div class="bg-orb bg-orb-2"></div>
    <div class="bg-orb bg-orb-3"></div>

    {{-- Scroll Progress --}}
    <div id="scroll-progress"></div>

    {{-- Page Transition --}}
    <div id="page-transition">
        <div class="pt-curtain"></div>
        <div class="pt-curtain-2"></div>
        <div class="pt-particles" id="pt-particles"></div>
    </div>
    <div id="pt-logo">
        <div class="pt-logo-ring">
            <svg fill="none" stroke="currentColor" stroke-width="1.5" viewBox="0 0 24 24">
                <rect x="2" y="3" width="20" height="14" rx="2"/>
                <path d="M8 21h8M12 17v4"/>
            </svg>
        </div>
    </div>

    {{-- Toast Container --}}
    <div id="toast-container"></div>

    {{-- ────────── NAVBAR ────────── --}}
    <header id="navbar">
        <div class="nav-inner">

            <a href="/spekpc" class="nav-brand" data-nav>
                <div class="brand-icon">
                    <svg fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
                        <rect x="2" y="3" width="20" height="14" rx="2"/>
                        <path d="M8 21h8M12 17v4"/>
                    </svg>
                </div>
                <span class="brand-text">MasterIP</span>
                <span class="brand-badge">BETA</span>
            </a>

            <ul class="nav-links">
                <li>
                    <a href="/dashboard" class="{{ request()->is('dashboard*') ? 'active' : '' }}" data-nav>
                        <span class="nav-fill"></span>
                        <svg fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
                            <rect x="3" y="3" width="7" height="7"/><rect x="14" y="3" width="7" height="7"/>
                            <rect x="14" y="14" width="7" height="7"/><rect x="3" y="14" width="7" height="7"/>
                        </svg>
                        <span>Dashboard</span>
                    </a>
                </li>
                <li>
                    <a href="/spekpc" class="{{ request()->is('spekpc*') ? 'active' : '' }}" data-nav>
                        <span class="nav-fill"></span>
                        <svg fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
                            <rect x="2" y="3" width="20" height="14" rx="2"/>
                            <path d="M8 21h8M12 17v4"/>
                        </svg>
                        <span>Spek PC</span>
                    </a>
                </li>
                <li>
                    <a href="/network" class="{{ request()->is('network*') ? 'active' : '' }}" data-nav>
                        <span class="nav-fill"></span>
                        <svg fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
                            <circle cx="12" cy="12" r="10"/>
                            <path d="M2 12h20"/>
                            <path d="M12 2a15.3 15.3 0 0 1 4 10 15.3 15.3 0 0 1-4 10 15.3 15.3 0 0 1-4-10 15.3 15.3 0 0 1 4-10z"/>
                        </svg>
                        <span>Network</span>
                        <span class="nav-net-dot"></span>
                    </a>
                </li>
                <li>
                    <a href="/koneksitoko" class="{{ request()->is('koneksitoko*') ? 'active' : '' }}" data-nav>
                        <span class="nav-fill"></span>
                        <svg fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
                            <path d="M3 9l9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"/>
                            <polyline points="9 22 9 12 15 12 15 22"/>
                        </svg>
                        <span>Koneksi Toko</span>
                    </a>
                </li>
                <li>
                    <a href="/data" class="{{ request()->is('data*') ? 'active' : '' }}" data-nav>
                        <span class="nav-fill"></span>
                        <svg fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
                            <ellipse cx="12" cy="5" rx="9" ry="3"/>
                            <path d="M21 12c0 1.66-4 3-9 3s-9-1.34-9-3"/>
                            <path d="M3 5v14c0 1.66 4 3 9 3s9-1.34 9-3V5"/>
                        </svg>
                        <span>Data</span>
                    </a>
                </li>
                <li>
                    <a href="/clip" class="{{ request()->is('clip*') ? 'active' : '' }}" data-nav>
                        <span class="nav-fill"></span>
                        <svg fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
                            <path d="M16 4h2a2 2 0 0 1 2 2v14a2 2 0 0 1-2 2H6a2 2 0 0 1-2-2V6a2 2 0 0 1 2-2h2"/>
                            <rect x="8" y="2" width="8" height="4" rx="1" ry="1"/>
                        </svg>
                        <span>Clipboard</span>
                    </a>
                </li>

                <li><div class="nav-divider"></div></li>

                <li>
                    <div class="user-chip">
                        <div class="user-avatar">
                            {{ strtoupper(substr(session('user_nama', 'U'), 0, 2)) }}
                        </div>
                        <div>
                            <div class="user-name">{{ session('user_nama', 'User') }}</div>
                            <div class="user-role">{{ session('user_nik', '-') }}</div>
                        </div>
                    </div>
                </li>

                <li><div class="nav-divider"></div></li>

                <li>
                    <a href="/logout" class="nav-logout" id="logout-link">
                        <span class="nav-fill"></span>
                        <svg fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
                            <path d="M9 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h4"/>
                            <polyline points="16 17 21 12 16 7"/>
                            <line x1="21" y1="12" x2="9" y2="12"/>
                        </svg>
                        <span>Logout</span>
                    </a>
                </li>
            </ul>
        </div>
    </header>

    <main>
        @yield('content')
    </main>

    {{-- ────────── FOOTER ────────── --}}
    <footer>
        {{-- Wave separator --}}
        <div class="footer-separator">
            <svg viewBox="0 0 1440 120" preserveAspectRatio="none" xmlns="http://www.w3.org/2000/svg">
                <defs>
                    <linearGradient id="waveGrad" x1="0%" y1="0%" x2="100%" y2="0%">
                        <stop offset="0%"   stop-color="#060d1f" stop-opacity="0"/>
                        <stop offset="20%"  stop-color="#3b82f6" stop-opacity="0.15"/>
                        <stop offset="50%"  stop-color="#06b6d4" stop-opacity="0.2"/>
                        <stop offset="80%"  stop-color="#8b5cf6" stop-opacity="0.15"/>
                        <stop offset="100%" stop-color="#060d1f" stop-opacity="0"/>
                    </linearGradient>
                </defs>
                <!-- Wave 1 (back) -->
                <path fill="url(#waveGrad)" d="
                    M0,60 C180,20 360,100 540,60 C720,20 900,100 1080,60 C1260,20 1380,80 1440,60
                    L1440,120 L0,120 Z"/>
                <!-- Wave 2 (mid) -->
                <path fill="rgba(6,13,31,0.5)" d="
                    M0,80 C200,40 400,110 600,70 C800,30 1000,110 1200,70 C1300,50 1380,85 1440,80
                    L1440,120 L0,120 Z"/>
                <!-- Wave 3 (front) -->
                <path fill="rgba(4,10,25,0.97)" d="
                    M0,100 C240,70 480,120 720,95 C960,70 1200,120 1440,100
                    L1440,120 L0,120 Z"/>
            </svg>
        </div>

        <div class="footer-inner">
            <canvas id="footer-matrix"></canvas>
            <div class="footer-glow"></div>
            <div class="footer-grid-bg"></div>

            <div class="footer-content-wrap">

                <div class="footer-ticker">
                    <div class="ticker-track">
                        <span>MasterIP</span><span class="ticker-sep">◆</span>
                        <span>MONITORING IP</span><span class="ticker-sep">◆</span>
                        <span>MONITORING SPEC</span><span class="ticker-sep">◆</span>
                        <span>NETWORK MONITOR</span><span class="ticker-sep">◆</span>
                        <span>EDP ACCESS</span><span class="ticker-sep">◆</span>
                        <span>DATA MANAGEMENT</span><span class="ticker-sep">◆</span>
                        <span>MasterIP</span><span class="ticker-sep">◆</span>
                        <span>MONITORING IP</span><span class="ticker-sep">◆</span>
                        <span>MONITORING SPEC</span><span class="ticker-sep">◆</span>
                        <span>NETWORK MONITOR</span><span class="ticker-sep">◆</span>
                        <span>EDP ACCESS</span><span class="ticker-sep">◆</span>
                        <span>DATA MANAGEMENT</span><span class="ticker-sep">◆</span>
                    </div>
                </div>

                <div class="footer-grid">
                    <div class="footer-brand">
                        <div class="brand-text-footer">MasterIP</div>
                        <p>Platform manajemen Spec Komputer, Monitoring IP, dan Akses Monitoring EDP terpadu untuk operasional yang lebih efisien.</p>
                        <div class="footer-status">
                            <div class="status-item">
                                <div class="status-dot online"></div>
                                <span>System Online</span>
                            </div>
                            <div class="status-item">
                                <div class="status-dot active-blue"></div>
                                <span>Network Active</span>
                            </div>
                        </div>
                    </div>

                    <div class="footer-col">
                        <h4>Navigasi</h4>
                        <ul>
                            <li><a href="/spekpc" data-nav>Spek PC</a></li>
                            <li><a href="/network" data-nav>Network</a></li>
                            <li><a href="/data" data-nav>Data All</a></li>
                            <li><a href="/clip" data-nav>Clipboard</a></li>
                            <li><a href="/dashboard" data-nav>Dashboard</a></li>
                            <li><a href="/koneksitoko" data-nav>Koneksi Toko</a></li>
                        </ul>
                    </div>

                    <div class="footer-col">
                        <h4>Sistem</h4>
                        <ul>
                            <li><a href="#">Export Excel</a></li>
                            <li><a href="#">Export CSV</a></li>
                            <li><a href="/logout">Logout</a></li>
                        </ul>
                    </div>
                </div>

                <div class="footer-bottom-wrap">
                    <div class="footer-bottom">
                        <span>© {{ date('Y') }} MasterIP — All rights reserved.</span>
                        <div class="footer-heartbeat">
                            <div class="hb-line">
                                <svg viewBox="0 0 80 20">
                                    <polyline points="0,10 10,10 15,3 20,17 25,3 30,17 35,10 45,10 50,5 55,15 60,10 80,10"/>
                                </svg>
                            </div>
                        </div>
                        <div class="footer-by">
                            <span>Crafted by</span>
                            <strong>Zmie</strong>
                            <span class="footer-version">v1.0 Beta</span>
                        </div>
                    </div>
                </div>

            </div>
        </div>
    </footer>

<script>
(function () {
    'use strict';

    /* ─── SCROLL PROGRESS ─── */
    const progressBar = document.getElementById('scroll-progress');
    function updateProgress() {
        const max = document.documentElement.scrollHeight - window.innerHeight;
        progressBar.style.width = max > 0 ? (window.scrollY / max * 100) + '%' : '0%';
    }
    window.addEventListener('scroll', updateProgress, { passive: true });

    /* ─── NAVBAR HIDE/SHOW ─── */
    let lastScroll = 0;
    const navbar = document.getElementById('navbar');
    window.addEventListener('scroll', () => {
        const cur = window.pageYOffset;
        if (cur <= 10)                navbar.classList.remove('hidden-bar');
        else if (cur > lastScroll + 6) navbar.classList.add('hidden-bar');
        else if (cur < lastScroll - 6) navbar.classList.remove('hidden-bar');
        lastScroll = cur;
    }, { passive: true });
    document.addEventListener('mousemove', e => {
        if (e.clientY < 64) navbar.classList.remove('hidden-bar');
    });

    /* ─── PAGE TRANSITION — ultra-fast ─── */
    const overlay   = document.getElementById('page-transition');
    const ptLogo    = document.getElementById('pt-logo');
    const ptParticles = document.getElementById('pt-particles');

    function spawnParticles() {
        ptParticles.innerHTML = '';
        const cx = window.innerWidth / 2, cy = window.innerHeight / 2;
        for (let i = 0; i < 24; i++) {
            const p = document.createElement('div');
            p.className = 'pt-particle';
            const angle = (i / 24) * Math.PI * 2;
            const dist  = 80 + Math.random() * 120;
            p.style.cssText = `
                left: ${cx}px; top: ${cy}px;
                --tx: ${Math.cos(angle) * dist}px;
                --ty: ${Math.sin(angle) * dist}px;
                background: ${Math.random() > 0.5 ? '#06b6d4' : '#3b82f6'};
                animation-delay: ${Math.random() * 0.15}s;
                animation-duration: ${0.35 + Math.random() * 0.2}s;
            `;
            ptParticles.appendChild(p);
        }
    }

    function animateOut(href) {
        spawnParticles();
        overlay.classList.remove('fade-out');
        overlay.classList.add('active');
        ptLogo.classList.add('visible');
        // Navigate after curtain covers screen (320ms)
        setTimeout(() => { window.location.href = href; }, 340);
    }

    // Entry animation — reveal from top fast
    overlay.classList.add('active');
    requestAnimationFrame(() => {
        requestAnimationFrame(() => {
            overlay.classList.remove('active');
            overlay.classList.add('fade-out');
            ptLogo.classList.remove('visible');
        });
    });

    document.querySelectorAll('[data-nav]').forEach(link => {
        link.addEventListener('click', function (e) {
            const href = this.getAttribute('href');
            if (!href || href === '#') return;
            e.preventDefault();
            animateOut(href);
        });
    });

    const logoutLink = document.getElementById('logout-link');
    if (logoutLink) {
        logoutLink.addEventListener('click', function (e) {
            e.preventDefault();
            animateOut(this.getAttribute('href'));
        });
    }

    /* ─── STAGGERED PAGE-IN ─── */
    function staggerIn() {
        const items = document.querySelectorAll(
            'main > *:not(.nm-modal-overlay):not(.kt-modal-overlay):not([id$="-modal"])'
        );
        items.forEach((el, i) => {
            el.style.opacity = '0';
            el.style.transform = 'translateY(18px)';
            el.style.transition = `opacity 0.42s ease ${0.04 + i * 0.06}s, transform 0.42s cubic-bezier(0.23,1,0.32,1) ${0.04 + i * 0.06}s`;
        });
        setTimeout(() => {
            items.forEach(el => { el.style.opacity = '1'; el.style.transform = 'translateY(0)'; });
        }, 60);
    }
    window.addEventListener('load', staggerIn);

    /* ─── INTERSECTION OBSERVER ─── */
    const revealObserver = new IntersectionObserver(entries => {
        entries.forEach(entry => {
            if (entry.isIntersecting) {
                entry.target.style.opacity = '1';
                entry.target.style.transform = 'translateY(0)';
                revealObserver.unobserve(entry.target);
            }
        });
    }, { threshold: 0.10, rootMargin: '0px 0px -30px 0px' });

    setTimeout(() => {
        document.querySelectorAll('.card, .glass-card, [data-reveal], table, .stat-box').forEach(el => {
            if (!el.style.opacity || el.style.opacity === '1') {
                el.style.opacity = '0';
                el.style.transform = 'translateY(16px)';
                el.style.transition = 'opacity 0.45s ease, transform 0.45s cubic-bezier(0.23,1,0.32,1)';
            }
            revealObserver.observe(el);
        });
    }, 500);

    /* ─── TOAST SYSTEM ─── */
    window.MasterIPToast = function(message, type = 'info', duration = 3500) {
        const container = document.getElementById('toast-container');
        const icons = {
            info: `<svg fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><circle cx="12" cy="12" r="10"/><line x1="12" y1="8" x2="12" y2="12"/><line x1="12" y1="16" x2="12.01" y2="16"/></svg>`,
            ok:   `<svg fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><polyline points="20 6 9 17 4 12"/></svg>`,
        };
        const toast = document.createElement('div');
        toast.className = 'toast';
        toast.innerHTML = `<div class="toast-icon ${type}">${icons[type] || icons.info}</div><span>${message}</span>`;
        container.appendChild(toast);
        requestAnimationFrame(() => requestAnimationFrame(() => toast.classList.add('show')));
        setTimeout(() => {
            toast.classList.remove('show');
            setTimeout(() => toast.remove(), 350);
        }, duration);
    };

    if (!sessionStorage.getItem('master_welcomed')) {
        setTimeout(() => {
            window.MasterIPToast('Selamat datang di MasterIP 👋', 'ok', 3000);
            sessionStorage.setItem('master_welcomed', '1');
        }, 800);
    }

    /* ─── MAGNETIC NAV LINKS ─── */
    document.querySelectorAll('.nav-links a, .nav-logout').forEach(el => {
        el.addEventListener('mousemove', function(e) {
            const rect = this.getBoundingClientRect();
            const dx = (e.clientX - (rect.left + rect.width / 2)) * 0.10;
            const dy = (e.clientY - (rect.top + rect.height / 2)) * 0.10;
            this.style.transform = `translate(${dx}px, ${dy - 2}px)`;
        });
        el.addEventListener('mouseleave', function() { this.style.transform = ''; });
    });

    /* ─── FOOTER MATRIX RAIN ─── */
    (function() {
        const canvas = document.getElementById('footer-matrix');
        if (!canvas) return;
        const footer = canvas.parentElement;
        const ctx = canvas.getContext('2d');

        function resize() {
            canvas.width  = footer.offsetWidth;
            canvas.height = footer.offsetHeight;
        }
        resize();
        window.addEventListener('resize', resize);

        const chars = '01アイウエオカキクケコMasterIP';
        const fontSize = 11;
        let cols = Math.floor(canvas.width / fontSize);
        let drops = Array(cols).fill(0).map(() => Math.random() * -50);

        function draw() {
            ctx.fillStyle = 'rgba(4,10,25,0.05)';
            ctx.fillRect(0, 0, canvas.width, canvas.height);
            ctx.fillStyle = '#06b6d4';
            ctx.font = fontSize + 'px JetBrains Mono, monospace';
            for (let i = 0; i < drops.length; i++) {
                const char = chars[Math.floor(Math.random() * chars.length)];
                ctx.fillText(char, i * fontSize, drops[i] * fontSize);
                if (drops[i] * fontSize > canvas.height && Math.random() > 0.975) drops[i] = 0;
                drops[i]++;
            }
        }
        setInterval(draw, 55);
    })();

})();
</script>
</body>
</html>